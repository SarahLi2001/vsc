// when a user joins a chat
new User('username');
users.push('username');

// when a user changes their username
users['old_username'] = 'new_username';
const usernameChange = (old_username) => new_username

// message from other user
const username = 'message'
new Message(username) = 'message'

// message from self
function Message() {
  'message'
}
